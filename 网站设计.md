请对我的个人网页进行整体的视觉风格重构与升级，请保留原有的履历信息和基本模块，但完全重写或调整 CSS 样式。

具体视觉升级规范如下：

1. 色彩规范 (Color Palette)：
   - 全局背景色 (Background) 改为深邃的纯黑色：#0d0d0d
   - 全局主文字色 (Text Primary) 改为柔和的白色：#f5f5f5
   - 全局次要文字色 (Text Secondary) 改为中灰：#888888
   - 全局核心高亮点缀色 (Accent Color) 使用荧光青柠绿：#c6ff00 (主要用于重要数字、标签、核心行动按钮和高亮 icon)

2. 卡片与布局样式 (Card Design)：
   - “专业优势”和“精选作品”等区域的卡片，背景色设为：rgba(255, 255, 255, 0.03)，并带有 1px 极细的半透明边框 border: 1px solid rgba(255, 255, 255, 0.08)。
   - 所有卡片设置优雅的圆角：border-radius: 20px;
   - 增加卡片间距和外边距，拒绝拥挤，给页面大量的呼吸空间 (Margins and Padding 至少 60px 以上)。

3. 高级微交互动效 (Micro-interactions)：
   - 为所有可交互的卡片和按钮添加悬停 (Hover) 状态：
     当鼠标悬停在“优势卡片”上时，卡片背景色平滑平移或渐变成纯荧光绿（#c6ff00），内部文字颜色反转变成深黑色（#0d0d0d），并带有一点点向上浮动的效果。
   - 所有悬停和过渡效果必须添加平滑的贝塞尔曲线过渡：transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);

4. 排版与字体 (Typography)：
   - 模块大标题（如 WORK EXPERIENCE, CORE STRENGTHS）使用大字号、全大写、粗体，并紧跟一个向下或向斜下方的箭头符号 (如 ↘ 或 ↓)。
   - 数据指标（如 8+, 30+）字号放大到 48px 以上，使用粗体加粗，并用荧光绿高亮。

请根据以上美学标准，重新生成这个页面的 HTML 和 CSS 代码。
“卡片交互”的代码原理：
/* 默认情况下的暗色卡片 */
.strength-card {
  background-color: rgba(255, 255, 255, 0.03);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 24px;
  padding: 40px;
  color: #ffffff;
  /* 丝滑过渡的核心 */
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1); 
}

/* 鼠标悬停（Hover）时的神奇变色 */
.strength-card:hover {
  background-color: #c6ff00; /* 变成荧光绿 */
  color: #0d0d0d;            /* 文字变成黑色，形成超高对比度 */
  transform: translateY(-8px); /* 卡片微微上浮 */
  box-shadow: 0 20px 40px rgba(198, 255, 0, 0.15); /* 带有绿色的软阴影 */
}